{
  "name": "barebones-markdown-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build --bundles nsis",
    "build:windows": "tauri build --bundles nsis",
    "build:mac": "tauri build --bundles app,dmg",
    "test": "node --test dist/viewer.test.js",
    "test:window-close": "tauri build --no-bundle && cargo.exe test --manifest-path src-tauri/Cargo.toml --test window_close -- --nocapture",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  },
  "dependencies": {
    "@tauri-apps/plugin-dialog": "^2.7.0"
  }
}
